# LibChecker organization profile

[`profile/README.md`](profile/README.md) is displayed on the [LibChecker organization homepage](https://github.com/LibChecker).

The architecture diagram uses GitHub's native Mermaid rendering. Keep its nodes and the repository table in sync when repositories change. Verify relationships against repository source and workflows before adding arrows; distinguish data flow from manual editing and documentation links. Private repositories are listed by name and high-level responsibility only, with explicit access labels.
